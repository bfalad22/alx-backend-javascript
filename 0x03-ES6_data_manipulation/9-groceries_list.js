const groceriesList = () => {
	const res = new Map();
	const objects = {
		Apples:10,
		Tomatoes: 10,
		Pasta: 1,
		Rice: 1,
		Banana: 5,
	};
	for (const key of object.keys(object)) {
		res.set(key, object[key]);
	}
	return res;
};
export default groceriesList;







